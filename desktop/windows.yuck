(defwindow desktop
    :monitor 0
    :geometry (geometry
        :width "100%"
        :height "100%"
        :anchor "center"
    )
    :stacking "bg" ;; putting this in foreground makes the mouse useless
    (box
        :vexpand true
        :hexpand true
        (box
            :valign "start"
            :halign "start"
            ( clock :class "clock")
        )
        
        (box 
        )
        (pc-info)
    )
)
